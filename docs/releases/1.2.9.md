# Markdown 1.2.9

- 更新重启后自动恢复已打开的文件、标签顺序、当前标签和编辑/预览模式，并保留侧栏目录。
- 保留未保存内容的确认流程；取消保存会停止退出，选择不保存则恢复磁盘上的文件内容。
- 文件缺失时提示错误并继续恢复其他文件，恢复记录仅使用一次。

此功能需要更新前运行的版本已包含恢复逻辑。首次从旧版升级到本版本时，无法补回旧版未记录的文件列表；后续升级即可自动恢复。

点击安装更新后将自动下载、验证并重启；未保存内容会先提示保存。
需要 macOS 26 或更新版本及 Apple Silicon。
