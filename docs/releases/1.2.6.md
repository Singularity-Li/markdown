# Markdown 1.2.6

增加安装暂时受阻时的自动重试：
- 用户确认安装后，安装助手启动、连接或写入等环节失败时，间隔 15、30、45 秒最多重试 3 次。
- 等待期间可取消；同一已确认版本自动继续，必要时重新下载并验签。
- 用户取消、拒绝授权、签名或有效性验证失败等情况不自动重试。
本次覆盖 App 仍在运行时收到的安装错误；App 已退出后的阻断不在覆盖范围内，企业防护软件兼容性仍需在对应设备验证。

点击安装更新后将自动下载、验证并重启；未保存内容会先提示保存。
需要 macOS 26 或更新版本及 Apple Silicon。
