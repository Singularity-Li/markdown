# Markdown 1.2.5

统一 App 图标为纯透明背景的双色 MD 字标，移除玻璃底板和底板边框。
“关于”与运行中的 Dock 直接使用包内同一份图标；图标资源指纹随内容更新，启动时重新注册安装包。
增加实际交付图标的透明背景校验，并将透明图标要求写入开发规范。
安装或升级后请启动新版一次；Finder 和未运行时的 Dock 可能仍需等待 macOS 缓存更新。

点击安装更新后将自动下载、验证并重启；未保存内容会先提示保存。
需要 macOS 26 或更新版本及 Apple Silicon。
