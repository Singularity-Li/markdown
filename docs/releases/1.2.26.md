# Markdown 1.2.26

- 统一应用图标为不透明浅灰渐变圆角底板，保留深蓝 M 与酒红 D 字标。
- Dock 运行图标与包内图标采用同一底板，修复启动后背景变透明的问题。
- 验证所有图标尺寸的不透明底板、双色字标和内容指纹。

安装替换后请启动新版一次；Finder 与未运行时 Dock 的缓存刷新时间由 macOS 管理。

点击安装更新后将自动下载、验证并重启；未保存内容会先提示保存。
需要 macOS 26 或更新版本及 Apple Silicon。
