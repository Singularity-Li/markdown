# 项目开发规范

## 沟通

- 始终称呼用户为 Tony，默认使用中文沟通。

## 界面设计规范

- 整个界面设计要保持简洁、一致、美观，避免软件界面臃肿。

## 统一强调色规范

- 主强调色使用 App 图标字母 D 的酒红色：sRGB `#A63D50`（166, 61, 80）。编辑选中态、标签指示、侧栏选中态、拖放提示、滑块及编辑器光标/选区默认使用主强调色或其透明度变体。
- 次强调色使用 App 图标字母 M 的深蓝色：sRGB `#203B5B`（32, 59, 91）。编辑/预览分段开关的选中块统一使用主强调色；后续界面确需第二种强调色时也使用此色，不另引入其他品牌强调色。未选中态仍保持中性色，不把次强调色当作所有次要文字的颜色。
- SwiftUI 与 AppKit 统一引用 `Sources/Markdown/AppTheme.swift`：主色为 `AppTheme.accent` / `accentNSColor`，次色为 `AppTheme.secondaryAccent` / `secondaryAccentNSColor`。正文链接与复选框使用常规 Markdown 蓝色，由 `Resources/style.css` 的 `--markdown-accent` 按明暗模式适配，不随软件酒红主色改变。新增界面须复用这些定义；图标生成源中的 D、M 颜色保持同步。
- 正文原始颜色、代码语法高亮及具有独立含义的错误/警告等语义颜色不属于品牌强调色，保持内容含义与可读性。系统管理的菜单、文件选择器等遵循 macOS 外观，不修改用户系统强调色设置。
- 编辑/预览采用带文字的 macOS 原生分段选择控件，保留原生交互，由 ModeSegmentedControl 显式绘制主色选中块和白色选中文字，未选中文字随系统外观适配；不依赖可能被 macOS 忽略的 tint 或 selectedSegmentBezelColor。原生控件优先保证可读性与系统交互一致性。
- 修改强调色须检查编辑/预览、标签、侧栏、透明度控件、链接及文本选区，确保明暗外观均能辨认；CSS 改动须重新生成内嵌资源再构建。

- 标签页、工具栏按钮和编辑/预览开关统一引用 `AppTheme.toolbarControlHeight`（24 点）及对应圆角；原生控件须验证 SwiftUI 宿主中的实际高度。选中块完整覆盖对应半边，不添加两端或上下内缩；像素检查须覆盖明暗外观。

## 每次完成工程修改任务时必须执行

1. 运行与本次修改相关的检查；默认运行 `bash scripts/test.sh`，确保通过。
2. 运行 `bash scripts/build_app.sh`，基于最新源码编译、签名并验证 App。
3. 仅在源码仓库根目录直接组装、签名并验证 `Markdown.app`，完整替换原版本。根目录是唯一 App 产出位置，该 App 必须纳入 Git。禁止在 `dist/`、`tmp/`、桌面或其他任何位置生成、复制或组装 App，包括中转包、测试副本与备份包。
4. 检查本次改动，将源码、开发规范、必要资源及根目录 `Markdown.app` 一起提交到 Git；提交说明应概括实际修改。
5. 自动推送到当前分支对应的远程分支。已有 upstream 时执行 `git push`；没有 upstream 时推送到 `origin` 的同名分支并设置 upstream。Tony 已授权这一常规提交和推送流程，无需每次重复确认。
6. 默认在提交并推送远程后结束交付，不自动发布新版本；源码、资源、文档及开发规范修改均适用。只有 Tony 明确要求发布新版本时，才执行下方“Agent 发布新版本流程”。
7. 验证推送成功后再报告完成，说明关键改动、验证结果及提交信息。明确要求发布的任务还须验证发布成功并报告版本号、构建号及 Release 链接；发布受阻时明确报告已完成步骤及阻碍，不得只推送后就宣称发布完成。

## 执行约束

- 编译、检查或签名验证失败时，先修复，不得将旧 App 当成新版本提交。
- 推送遇到认证、网络或远程冲突时，保留本地提交并明确报告阻碍；不得声称已同步，也不得强制推送覆盖远程历史。
- 不提交构建缓存、临时文件、备份 App 或密钥；不混入与任务无关的用户改动。
- `dist/` 和 `.build/` 仅用于非 App 的资源与编译中间产物，继续忽略；仓库根目录 `Markdown.app` 必须被追踪。
- 实际界面验证使用根目录 App，不创建独立测试 App 副本；重启前保护用户未保存内容。
- 开发完成后仅更新源码仓库根目录的 `Markdown.app`；不得自动向桌面复制、替换或更新 App、示例文件、图片等任何文件。只有 Tony 当次明确要求时才操作桌面文件。

## 自动更新与发布边界

- 根目录唯一 App 限制针对本项目的开发构建产物；发布脚本允许在忽略的 `dist/` 中直接压缩根目录 App，但不得生成另一份展开的 Markdown.app。
- Sparkle 依赖内部的安装辅助程序属于第三方框架组件，允许在依赖缓存及根目录 App 的嵌入框架中存在；不得当作项目 App 副本删除。
- 最终用户主动升级时，更新器可在系统缓存中下载、验签、解压及暂存替换，并更新正在运行的安装位置。此运行时流程不等同于开发完成后自动复制桌面文件。

## Git 忽略规范

- 每次完成开发时检查新增文件，把非源码、非交付 App 且不适合版本管理的内容加入 `.gitignore`。
- 忽略构建缓存与中间产物、日志、临时文件、测试报告、调试截图、备份、编辑器个人配置以及本地环境和密钥文件。
- 保留构建所需资源、脚本、测试、示例、依赖声明、README 和开发规范；不得简单地把所有非源码文件都忽略。
- 根目录 `Markdown.app` 是必须提交的例外；其他位置的 App 和备份不提交。
- 提交前检查暂存区，确认忽略规则生效且交付 App 已被追踪。若不适合追踪的内容已经进入 Git，仅移除其追踪，保留本地文件；涉及无关用户文件时不要擅自修改。

## App 图标更新规范

- 图标采用纯透明背景的双色 MD 字标，不添加玻璃底板、圆角底块、底板描边或阴影。所有 ICNS 尺寸保持一致；Dock 与“关于”直接读取包内同一图标资源，Finder 通过该资源注册显示。构建时验证实际 ICNS 的透明区域和可见字标。

- 图标修改必须落实到资源生成源 `scripts/make_assets.swift`，再通过 `bash scripts/build_app.sh` 生成并交付；不得只替换已打包 App 内的图标。
- 每次构建必须递增 `CFBundleVersion`，不得固定或回退构建号；替换根目录 App 前保留其构建号作为递增依据。合并分支或恢复旧包后，交付构建号仍须高于上一交付版本。
- 图标资源使用实际 ICNS 内容的 SHA-256 指纹命名：`AppIcon-<指纹>.icns`；`CFBundleIconFile` 必须指向对应资源名。图标内容改变时资源名必须改变，不得恢复为固定的 `AppIcon.icns`。完整重建包，避免残留旧图标资源。
- 图标生成或转换失败必须中止构建，不得跳过、使用缓存旧图标或继续签名交付。图标及 Info.plist 必须在签名前写入，签名后不得修改包内容。
- 保留 `AppIconRefresh.refresh()` 的启动调用：直接从当前安装包读取图标并设置运行中的 Dock 图标；首次启动、构建号或图标资源名变化、安装路径变化时，更新当前包的 Launch Services 注册信息。仅注册成功后保存标识，失败应记录并在下次启动重试；未变化时不重复注册。
- 图标或刷新逻辑修改后，必须运行 `bash scripts/test.sh`，覆盖首次注册、重复启动、版本变化、图标变化、安装路径变化及失败重试；构建脚本必须执行 `Tests/Icon/verify_bundle.swift`，验证实际交付包的图标可解码、内容指纹与资源名一致、构建号有效，并通过签名验证。
- 实际启动验证只使用根目录 App，确认当前版本、图标及注册结果；重启前保护未保存内容。没有在另一台电脑实际验证时，不得声称跨机缓存问题已完全解决。
- 安装说明应提醒替换后启动新版一次。Finder 和未运行时的 Dock 图标仍受 macOS 缓存管理，不得承诺安装后立即刷新；不得为刷新图标自动清空系统缓存、重启 Finder / Dock、修改 Bundle ID 或更改用户的默认文件关联。

## 更新安装重试规范

- 对用户已确认版本的暂时安装错误，最多自动重试 3 次，分别等待 15、30、45 秒；提供取消入口，在上一更新周期结束后才启动重试，避免并发更新。
- 自动继续必须同时匹配用户批准的版本号和下载地址；版本或地址变化重新确认。每次仍由 Sparkle 下载、验签和安装，不绕过企业安全软件或替代系统授权。
- 用户取消/拒绝授权、签名与有效性验证失败、降级、网络下载错误不自动安装重试；错误链也须检查。重试耗尽恢复明确错误提示，不无限循环。
- 修改重试逻辑须覆盖连续拦截、次数上限、取消、不同版本/地址及嵌套签名错误。模拟测试不等于在企业拦截软件上实测；不得宣称主进程退出后的安装错误也能被主进程重试。

## Agent 发布新版本流程

- 默认不发布新版本，只完成常规检查、构建、提交和远程推送。只有 Tony 明确说“发布新版本”“发布一个版本”或指定版本发布时，才授权执行准备、测试、构建、更新签名、Git 提交与推送、GitHub 正式 Release 发布及公开下载验证；已明确要求发布后无需再次确认。
- 获得明确发布要求后，同一任务内的多项修改合并发布一次，不因中间提交或发布流程自身产生的版本文件、更新说明及 App 提交再次递归发版。此前任务的发布要求不延续为后续任务的自动发布授权。纯问答、只读检查或普通工程修改均不自行触发发布；Tony 明确要求暂不发布时遵循其要求。
- 入口：`python3 scripts/release.py`。默认在现有正式版基础上递增补丁号；若 VERSION 已提高则使用该版本；首次发布沿用 VERSION。指定版本使用 `--version X.Y.Z`，不得回退版本或构建号。
- 发布前审查工作区，先完成并提交已授权开发；有无关用户改动时不得混入。根据实际改动撰写简洁中文说明到忽略的 `dist/release-notes.md`，传入 `--notes-file dist/release-notes.md`；无自定义说明时脚本从提交记录生成。
- 必须使用脚本完成全链路，不手工跳过测试、签名或附件验证。发布仅从默认分支进行；脚本检查远程历史、钥匙串公钥、版本单调性，构建并压缩根目录 App，不另外组装 App。
- 更新清单和更新包均由 Sparkle 工具签名，公钥固定在 `Resources/UpdatePublicKey.txt`，私钥属于钥匙串账户 `com.tony.markdown`。不得输出、提交或上传私钥；换电脑必须安全迁移原密钥，不能自动生成新密钥替代。
- 更新清单遵循最小化原则：只保留 RSS 容器、构建号、展示版本号、最低系统版本、硬件要求，以及更新包下载 URL、长度、类型和签名。完整更新说明放在 GitHub Release 与 `docs/releases/`，不嵌入 appcast；不得删除 Sparkle 生成的清单签名或签名相关注释，签名后不再修改内容。
- 每个正式 Release 必须包含 `Markdown-X.Y.Z.zip` 和已签名的 `appcast.xml`。先创建草稿、上传并下载校验，再发布为 latest，最后验证匿名下载和 latest 清单入口。不得覆盖已发布附件或强制改写 tag。
- 失败保留本地提交、草稿及 `dist/releases/vX.Y.Z/state.json`，使用 `python3 scripts/release.py --resume vX.Y.Z` 恢复同一次发布；不要盲目递增版本重发。`--prepare-only` 仅准备，不能据此宣称已发布。
- 发布完成报告版本、构建号、提交、Release 链接和验证范围。首次支持更新的版本需手动安装；后续才能从 App 一键升级。当前为 ad-hoc 签名，不能声称已 Apple 公证。
