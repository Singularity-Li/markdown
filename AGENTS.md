# 项目开发规范

## 沟通

- 始终称呼用户为 Tony，默认使用中文沟通。

## 界面设计规范

- 整个界面设计要保持简洁、一致、美观，避免软件界面臃肿。

## 每次完成开发时必须执行

1. 运行与本次修改相关的检查；默认运行 `bash scripts/test.sh`，确保通过。
2. 运行 `bash scripts/build_app.sh`，基于最新源码编译、签名并验证 App。
3. 将新版本放在源码仓库根目录的 `Markdown.app`，完整替换原版本，只保留这一份最新产物。该 App 必须纳入 Git，不能仅保存在桌面或被忽略的 `dist/` 目录。
4. 检查本次改动，将源码、开发规范、必要资源及根目录 `Markdown.app` 一起提交到 Git；提交说明应概括实际修改。
5. 自动推送到当前分支对应的远程分支。已有 upstream 时执行 `git push`；没有 upstream 时推送到 `origin` 的同名分支并设置 upstream。Tony 已授权这一常规提交和推送流程，无需每次重复确认。
6. 验证推送成功后再报告完成，说明关键改动、验证结果及提交信息。

## 执行约束

- 编译、检查或签名验证失败时，先修复，不得将旧 App 当成新版本提交。
- 推送遇到认证、网络或远程冲突时，保留本地提交并明确报告阻碍；不得声称已同步，也不得强制推送覆盖远程历史。
- 不提交构建缓存、临时文件、备份 App 或密钥；不混入与任务无关的用户改动。
- `dist/` 和 `.build/` 继续忽略；仓库根目录 `Markdown.app` 必须被追踪。
- 开发完成后仅更新源码仓库根目录的 `Markdown.app`；不得自动向桌面复制、替换或更新 App、示例文件、图片等任何文件。只有 Tony 当次明确要求时才操作桌面文件。

## Git 忽略规范

- 每次完成开发时检查新增文件，把非源码、非交付 App 且不适合版本管理的内容加入 `.gitignore`。
- 忽略构建缓存与中间产物、日志、临时文件、测试报告、调试截图、备份、编辑器个人配置以及本地环境和密钥文件。
- 保留构建所需资源、脚本、测试、示例、依赖声明、README 和开发规范；不得简单地把所有非源码文件都忽略。
- 根目录 `Markdown.app` 是必须提交的例外；其他位置的 App 和备份不提交。
- 提交前检查暂存区，确认忽略规则生效且交付 App 已被追踪。若不适合追踪的内容已经进入 Git，仅移除其追踪，保留本地文件；涉及无关用户文件时不要擅自修改。
